## ADDED Requirements

*(No new capabilities — this change is a pure implementation bug fix within existing radar-data-processing behavior.)*

## MODIFIED Requirements

*(No spec-level requirement changes — the existing specification already describes the intended behavior. This change corrects the implementation to match it.)*
